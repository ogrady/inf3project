package db;

/**
 * Query for an object by its unique id
 * 
 * @author Daniel
 *
 * @param <I>
 */
public class IdQuery<I extends Idable> {
	/*
	 * private ObjectContainer db;
	 * 
	 * public IdQuery(ObjectContainer _db) { db = _db; }
	 * 
	 * public I getById(final int _id) { ObjectSet<I> results = db.query(new
	 * Predicate<I>() { private static final long serialVersionUID = 1L;
	 * 
	 * @Override public boolean match(I _idable) { return _idable.getId() == _id; }
	 * }); return !results.isEmpty() ? results.get(0) : null; }
	 */
}
